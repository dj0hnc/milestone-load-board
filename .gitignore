node_modules/
dist/
dist-pkg/
.board-backups/
# the real config carries Samsara tokens — NEVER commit it (CI builds it from the template + secrets)
newmile.config.json

# local build/diagnostic junk
_build.log
_chk_board.js

.commitmsg.tmp

# cloud report engine: the self-rotating NewMile token lives in Actions cache, NEVER in the repo
report-engine/.state/
